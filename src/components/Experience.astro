---
import { experienceList } from '../data/experience';
import ExperienceItem from './ui/ExperienceItem.astro';
---

<section id="experience" class="py-12 sm:py-16 scroll-mt-16">
  <div class="space-y-6">
    <div class="space-y-1">
      <h2
        class="text-2xl sm:text-3xl font-bold text-(--color-text-primary)"
        style="font-family: var(--font-heading);"
      >
        Experience
      </h2>
      <p class="text-sm text-(--color-text-secondary)">
        My technical experience, including 13 months as a software engineer at Siemens and a Capital One-sponsored university project.
      </p>
    </div>

    <div class="space-y-4">
      {experienceList.map((exp) => (
        <ExperienceItem experience={exp} />
      ))}
    </div>
  </div>
</section>
